import:
  - /home/joe/.config/alacritty/dracula.yml
  - /home/joe/.config/alacritty/alacritty.yml

env:
  TERM: xterm-256color
window:
  dynamic_title: true
  dimensions:
    columns: 0
    lines: 0
  padding:
    x: 10
    y: 10
font:
  size: 12
  # Spread additional padding evenly around the terminal content.
  dynamic_padding: true
  decorations: full
  startup_mode: Windowed

  # Window class (Linux only):
  class: Alacritty
background_opacity: .90